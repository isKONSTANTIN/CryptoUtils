**What does this PR do?**
A short description of the change and why it's needed.

**How was this tested?**
`./gradlew test` / manual run / etc. — for a new or changed command, include the exact invocation you tested.

**Checklist**
- [ ] `./gradlew build` passes locally
- [ ] Tests added/updated for the behavior change (if applicable)
- [ ] README command table updated (if a command was added, removed or its arguments changed)
